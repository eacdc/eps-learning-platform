import 'package:intl/intl.dart';

class DateTimeHelper {
  /* static String timestampToDateTime(Timestamp? timestamp) {
    try {
      if (timestamp == null) {
        return "Invalid Timestamp"; // Return a default message for null input
      }
      DateTime dateTime = timestamp.toDate();
      return DateFormat('MMM dd, yyyy hh:mm a').format(dateTime);
    } catch (e) {
      // Handle any potential errors
      return "Error formatting date"; // Return an error message
    }
  } */

 /*  static String timestampToDateTime2(Timestamp? timestamp) {
    try {
      if (timestamp == null) {
        return "Invalid Timestamp"; // Return a default message for null input
      }
      DateTime dateTime = timestamp.toDate();
      return DateFormat('dd MMM, hh:mm a').format(dateTime);
    } catch (e) {
      // Handle any potential errors
      return "Error formatting date"; // Return an error message
    }
  } */
}
